{
  "name": "publishUpload",
  "version": "0.0.0",
  "description": "Alert Pub/Sub of an upload",
  "private": true,
  "scripts": {
    "deploy": "gcloud functions deploy publishUpload --runtime=nodejs16 --trigger-http --region=us-east4 --allow-unauthenticated"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.1.2",
    "@google-cloud/pubsub": "^3.0.1"
  },
  "volta": {
    "extends": "../../package.json"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
