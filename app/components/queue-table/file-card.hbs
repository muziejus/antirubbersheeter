<div class="p-3 w-1/2">
  <h3 class="text-3xl">
  {{#if @file}}
    {{@file.name}}
  {{else}}
    {{#if (eq @type "image")}}
      No map uploaded.
    {{else}}
      No spatial data uploaded.
    {{/if}}
  {{/if}}
  </h3>
</div>

